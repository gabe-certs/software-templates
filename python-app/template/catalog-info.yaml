apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: Python app displays the time.
  annotations:
    github.com/project-slug: gabe-certs/${{ values.name }}
spec:
  type: service
  owner: ${{ values.owner }}
  lifecycle: experimental